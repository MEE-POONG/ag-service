generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

type AdjustbetsCockfight {
  commissionRBG Int
  enabled       Boolean
  lastError     String?
  lastSavedAt   DateTime? @db.Date
  profile       Int
  statusServe   String?
  work          Boolean
}

type AdjustbetsLottoRcw {
  enabled     Boolean
  lastSavedAt DateTime? @db.Date
  share       Int
  statusServe String?
  work        Boolean
}

type AdjustbetsLottoRdc {
  enabled     Boolean
  lastSavedAt DateTime? @db.Date
  share       Int
  statusServe String?
  work        Boolean
}

type AdjustbetsMuayStep {
  enabled     Boolean
  /// Could not determine type: the field only had null or empty values in the sample set.
  lastError   Json?
  lastSavedAt DateTime? @db.Date
  profile     Int
  statusServe String?
  work        Boolean
}

type AdjustbetsSa {
  commissionRAR Int
  enabled       Boolean
  lastError     String?
  lastSavedAt   DateTime? @db.Date
  profile       Int
  statusServe   String?
  work          Boolean
}

type AdjustbetsSexy {
  enabled     Boolean
  lastError   String?
  lastSavedAt DateTime? @db.Date
  profile     Int
  statusServe String?
  work        Boolean
}

type AdjustbetsSlotItp {
  enabled     Boolean
  lastError   String?
  lastSavedAt DateTime? @db.Date
  statusServe String?
  work        Boolean
}

type AdjustbetsSlotJoker {
  enabled     Boolean
  lastError   String?
  lastSavedAt DateTime? @db.Date
  statusServe String?
  work        Boolean
}

type AdjustbetsSlotPlaystar {
  enabled Boolean
  work    Boolean
}

type AdjustbetsSportsbook {
  commission  AdjustbetsSportsbookCommission
  /// Could not determine type: the field only had null or empty values in the sample set.
  lastError   Json?
  lastSavedAt DateTime?                      @db.Date
  limits      AdjustbetsSportsbookLimits
  statusServe String?
  work        Boolean
}

type AdjustbetsSportsbookCommission {
  main  Int
  other Int
  par   Int
  x12   Int
}

type AdjustbetsSportsbookLimits {
  beforeRun       Int
  matchLimitOS    Int
  matchLimitOther Int
  matchLimitX12   Int
  maxOS           Int
  maxOther        Int
  maxPar          Int
  maxX12          Int
  par             Int
  transLimit      Int
}

type AdjustbetsVirtualSports {
  enabled     Boolean
  lastSavedAt DateTime? @db.Date
  profile     Int
  statusServe String?
  work        Boolean
}

type CreditTopUpAdviserDetails {
  createdAt   String
  createdBy   String
  gaSecretEnc String
  id          String
  isActive    Boolean
  /// Could not determine type: the field only had null or empty values in the sample set.
  meta        Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  note        Json?
  origin      String
  position    String
  reserve     String
  statusServe String
  updatedAt   String
  updatedBy   String
  userLogin   String
  username    String
  webname     String
}

model ActivityLogDB {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  action    String
  createdAt DateTime @db.Date
  ipAddress String?
  newData   String?
  recordId  String?
  tableName String?
  userAgent String?
  userId    String
  userType  String
}

model AdminDB {
  id              String          @id @default(auto()) @map("_id") @db.ObjectId
  adminPositionId String          @db.ObjectId
  adminPosition   AdminPositionDB @relation(fields: [adminPositionId], references: [id], onDelete: Cascade)
  webBaseId       String?         @db.ObjectId
  webBase         WebBaseDB?      @relation(fields: [webBaseId], references: [id], onDelete: Cascade)
  createdAt       DateTime        @db.Date
  createdBy       String
  deleteAt        DateTime?       @db.Date
  /// Could not determine type: the field only had null or empty values in the sample set.
  deleteBy        Json?
  email           String          @unique
  isActive        Boolean
  name            String
  password        String
  /// Could not determine type: the field only had null or empty values in the sample set.
  profile         Json?
  tel             String
  updatedAt       DateTime        @db.Date
  updatedBy       String
  username        String          @unique
  // Chat system relations
  assignedConversations ChatConversationDB[]
}

model AdminDefaultPermissionDB {
  id                String          @id @default(auto()) @map("_id") @db.ObjectId
  adminPositionDBId String          @db.ObjectId
  adminPosition     AdminPositionDB @relation(fields: [adminPositionDBId], references: [id], onDelete: Cascade)
  canAdvance        Boolean
  canCreate         Boolean
  canDelete         Boolean
  canUpdate         Boolean
  canViews          Boolean
  createdAt         DateTime        @db.Date
  createdBy         String
  isDeleted         Boolean
  menuPageWebId     String?         @db.ObjectId
  menuPage          MenuPageWebDB?  @relation(fields: [menuPageWebId], references: [id], onDelete: Cascade)
  updatedAt         DateTime        @db.Date
  updatedBy         String

  @@unique([adminPositionDBId, menuPageWebId])
}

model AdminDepartmentDB {
  id             String            @id @default(auto()) @map("_id") @db.ObjectId
  createdAt      DateTime          @db.Date
  createdBy      String
  description    String
  isActive       Boolean
  isDeleted      Boolean
  name           String            @unique
  updatedAt      DateTime          @db.Date
  updatedBy      String
  adminPositions AdminPositionDB[]
}

model AdminPositionDB {
  id                       String                     @id @default(auto()) @map("_id") @db.ObjectId
  adminDepartmentId        String                     @db.ObjectId
  adminDepartment          AdminDepartmentDB          @relation(fields: [adminDepartmentId], references: [id], onDelete: Cascade)
  createdAt                DateTime                   @db.Date
  createdBy                String
  isActive                 Boolean
  isDeleted                Boolean
  name                     String
  priority                 Int
  updatedAt                DateTime                   @db.Date
  updatedBy                String
  AdminDB                  AdminDB[]
  AdminDefaultPermissionDB AdminDefaultPermissionDB[]

  @@unique([adminDepartmentId, name], map: "uniq_position_name_per_dept")
  @@index([adminDepartmentId])
}

model AgQueueJobDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String?
  createdAt    DateTime @db.Date
  errorMessage String?
  eventDocId   String
  jobId        String   @unique(map: "jobId_1")
  priority     Int
  queueSize    Int
  status       String
  type         String
  updatedAt    DateTime @db.Date
  username     String?
}

model AgUserAccountDB {
  id               String            @id @default(auto()) @map("_id") @db.ObjectId
  createdAt        DateTime          @db.Date
  createdBy        String
  gaSecretEnc      String
  isActive         Boolean
  /// Could not determine type: the field only had null or empty values in the sample set.
  meta             Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  note             Json?
  origin           String
  position         String
  reserve          String
  statusServe      String
  updatedAt        DateTime          @db.Date
  updatedBy        String
  userLogin        String?
  username         String            @unique
  webname          String
  partnerAG      String?
  partnerLogin String?
  CustomerAgentDB  CustomerAgentDB[]
  PartnerDB        PartnerDB[]
}

model CustomerAgentDB {
  id              String          @id @default(auto()) @map("_id") @db.ObjectId
  agentId         String          @db.ObjectId
  agUserAccountDB AgUserAccountDB @relation(fields: [agentId], references: [id], onDelete: Cascade)
  createdAt       DateTime        @db.Date
  createdBy       String
  customerId      String          @unique
  isActive        Boolean
  note            String
  updatedAt       DateTime        @db.Date
  updatedBy       String
}

model ImageList {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime @db.Date
  createdBy String
  deleteBy  String
  imageUrl  String
  isDeleted Boolean
  modelName String
  nameFile  String
  updatedAt DateTime @db.Date
  updatedBy String
}

model MenuPageWebDB {
  id                       String                     @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  link                     Json?                      @unique
  /// Field referred in an index, but found no data to define the type.
  name                     Json?                      @unique
  AdminDefaultPermissionDB AdminDefaultPermissionDB[]
}

model MenuWebDB {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  canAdvance  Boolean
  canCreate   Boolean
  canDelete   Boolean
  canUpdate   Boolean
  canViews    Boolean
  createdAt   DateTime @db.Date
  createdBy   String
  description String
  head        Boolean
  icon        String?
  isDeleted   Boolean?
  isVisible   Boolean
  link        String
  manager     String[]
  name        String   @unique
  parentId    String?  @db.ObjectId
  showOrder   Int
  updatedAt   DateTime @db.Date
  updatedBy   String

  // Self-referential relations for menu hierarchy
  parent   MenuWebDB?  @relation("MenuParentChildren", fields: [parentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  children MenuWebDB[] @relation("MenuParentChildren")
}

model PartnerDB {
  id              String          @id @default(auto()) @map("_id") @db.ObjectId
  agentId         String          @db.ObjectId
  agUserAccountDB AgUserAccountDB @relation(fields: [agentId], references: [id], onDelete: Cascade)
  bankName        String
  bankNumber      String
  createdAt       DateTime        @db.Date
  createdBy       String
  line            String
  method          String
  name            String
  startDate       DateTime        @db.Date
  status          String
  tel             String
  updatedAt       DateTime        @db.Date
  updatedBy       String
}

model SetZero {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model SettingDB {
  id                String   @id @default(auto()) @map("_id") @db.ObjectId
  name              String
  logo              String
  logoText          String?
  logoTextColor     String
  logoTextSize      Int
  logoTextFont      String
  logoTextFontSize  Int
  logoTextFontColor String
  facebook          String?
  instagram         String?
  line              String?
  youtube           String?
  website           String?
  address           String?
  tel               String
  createdAt         DateTime @db.Date
  createdBy         String
  updatedAt         DateTime @db.Date
  updatedBy         String
}

model UnblockCustomerDB {
  id               String   @id @default(auto()) @map("_id") @db.ObjectId
  adviser          String
  createdAt        String
  createdBy        String
  customerUsername String?
  errorMessage     String
  /// Could not determine type: the field only had null or empty values in the sample set.
  errorStack       Json?
  ipAddress        String
  newPassword      String
  processedAt      DateTime @db.Date
  requestedBy      String
  status           String
  type             String
  updatedAt        DateTime @db.Date
  userAgent        String
  usernameAG       String
}

model UserDB {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime @db.Date
  createdBy String
  email     String   @unique
  firstname String
  isActive  Boolean
  isDeleted Boolean
  lastname  String
  password  String
  tel       String
  updatedAt DateTime @db.Date
  updatedBy String
  username  String   @unique
}

model WebBaseDB {
  id        String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime  @db.Date
  createdBy String
  isActive  Boolean
  isDeleted Boolean
  name      String    @unique
  otpA      String
  otpM      String
  otpS      String
  passA     String
  passM     String
  passS     String
  updatedAt DateTime  @db.Date
  updatedBy String
  AdminDB   AdminDB[]

  @@index([isActive], map: "isActive_1")
}

model WorkHistoryDB {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  action    String
  createdAt DateTime @db.Date
  errorMsg  String?
  ipAddress String
  isSuccess Boolean
  newData   String?
  oldData   String?
  recordId  String?  @db.ObjectId
  tableName String
  userAgent String
  userId    String
  userType  String

  @@index([tableName])
  @@index([recordId])
  @@index([userId])
  @@index([action])
  @@index([createdAt])
}

model adjustbets {
  id            String                  @id @default(auto()) @map("_id") @db.ObjectId
  agBaseUrl     String
  attemptCount  Int
  cockfight     AdjustbetsCockfight
  createdAt     DateTime                @db.Date
  createdBy     String
  customer      String
  lastError     String
  lottoRCW      AdjustbetsLottoRcw
  lottoRDC      AdjustbetsLottoRdc
  muayStep      AdjustbetsMuayStep
  overallStatus String
  pinUsed       String
  sa            AdjustbetsSa
  sexy          AdjustbetsSexy
  slotItp       AdjustbetsSlotItp
  slotJoker     AdjustbetsSlotJoker
  slotPlaystar  AdjustbetsSlotPlaystar
  sportsbook    AdjustbetsSportsbook
  updatedAt     DateTime                @db.Date
  updatedBy     String
  usernameAG    String
  virtualSports AdjustbetsVirtualSports
}

model admin {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  bankName   String
  bankNumber String
  createdAt  DateTime @db.Date
  /// Could not determine type: the field only had null or empty values in the sample set.
  deleted_at Json?
  firstname  String
  lastname   String
  line       String
  nickname   String
  password   String
  position   String
  team       String
  tel        String
  updatedAt  DateTime @db.Date
  username   String
}

model creditTopUp {
  id             String                     @id @default(auto()) @map("_id") @db.ObjectId
  adviser        String
  adviserDetails CreditTopUpAdviserDetails?
  agentId        String
  agentLogin     String
  createdAt      String
  createdBy      String
  credit         Int
  creditBy       String
  status         String
  statusAG       String?
  statusFlag     String
  statusServe    String
  type           String
  /// Multiple data types found: String: 20%, DateTime (Date): 80% out of 5 sampled entries
  updatedAt      Json
  updatedBy      String
  usernameAG     String
  webname        String
}

model user {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model ResetAgUserPasswordDB {
  id           String    @id @default(auto()) @map("_id") @db.ObjectId
  v            Int       @map("__v")
  adviser      String
  createdAt    DateTime  @db.Date
  errorMessage String?
  newPassword  String
  position     String
  processedAt  DateTime? @db.Date
  status       String
  updatedAt    DateTime  @db.Date
  usernameAG   String
}

model CreateResetMasterJobsDB {
  id            String   @id @default(auto()) @map("_id") @db.ObjectId
  v             Int      @map("__v")
  adviser       String
  createdAt     DateTime @db.Date
  insertedCount Int
  limit         Int
  newPassword   String
  processedAt   DateTime @db.Date
  status        String
  updatedAt     DateTime @db.Date
}

model UnblockMasterDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String
  createdAt    DateTime @db.Date
  errorMessage String
  newPassword  String
  position     String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

model MasterChangePasswordDB {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  v           Int      @map("__v")
  adviser     String
  createdAt   DateTime @db.Date
  masterLogin String
  newPassword String
  oldPassword String
  processedAt DateTime @db.Date
  status      String
  updatedAt   DateTime @db.Date
  usernameAG  String
}

model UnblockAgentDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String
  createdAt    DateTime @db.Date
  errorMessage String
  newPassword  String
  position     String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

model AgentChangePasswordDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String
  agentLogin   String?
  createdAt    DateTime @db.Date
  errorMessage String?
  errorStack   String?
  newPassword  String
  oldPassword  String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

// ==================== Customer Chat System Models ====================

// Chat Customer - represents customers who use the chat system
model ChatCustomerDB {
  id            String              @id @default(auto()) @map("_id") @db.ObjectId
  customerId    String              @unique // External customer ID
  name          String?
  email         String?
  phone         String?
  avatarUrl     String?
  // Custom fields for segmentation
  language      String              @default("th") // th, en
  timezone      String              @default("Asia/Bangkok")
  metadata      Json?               // Additional custom fields
  // Status
  isActive      Boolean             @default(true)
  isBlocked     Boolean             @default(false)
  blockedReason String?
  // Timestamps
  createdAt     DateTime            @default(now()) @db.Date
  updatedAt     DateTime            @updatedAt @db.Date
  lastSeenAt    DateTime?           @db.Date
  // Relations
  conversations ChatConversationDB[]
  messages      ChatMessageDB[]
  tags          ChatCustomerTagDB[]
  segments      ChatCustomerSegmentDB[]

  @@index([email])
  @@index([phone])
  @@index([isActive])
  @@index([createdAt])
}

// Chat Conversation - represents individual chat threads
model ChatConversationDB {
  id              String           @id @default(auto()) @map("_id") @db.ObjectId
  customerId      String           @db.ObjectId
  customer        ChatCustomerDB   @relation(fields: [customerId], references: [id], onDelete: Cascade)
  // Agent assignment
  assignedAdminId String?          @db.ObjectId
  assignedAdmin   AdminDB?         @relation(fields: [assignedAdminId], references: [id], onDelete: SetNull)
  // Status
  status          String           @default("open") // open, assigned, resolved, closed
  priority        String           @default("normal") // low, normal, high, urgent
  // Content
  subject         String?
  lastMessage     String?
  lastMessageAt   DateTime?        @db.Date
  // Flags
  isUnread        Boolean          @default(true)
  unreadCount     Int              @default(0)
  // Timestamps
  createdAt       DateTime         @default(now()) @db.Date
  updatedAt       DateTime         @updatedAt @db.Date
  resolvedAt      DateTime?        @db.Date
  closedAt        DateTime?        @db.Date
  // Relations
  messages        ChatMessageDB[]

  @@index([customerId])
  @@index([assignedAdminId])
  @@index([status])
  @@index([priority])
  @@index([createdAt])
  @@index([lastMessageAt])
}

// Chat Message - individual messages in conversations
model ChatMessageDB {
  id             String              @id @default(auto()) @map("_id") @db.ObjectId
  conversationId String              @db.ObjectId
  conversation   ChatConversationDB  @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  customerId     String              @db.ObjectId
  customer       ChatCustomerDB      @relation(fields: [customerId], references: [id], onDelete: Cascade)
  // Sender info
  senderType     String              // customer, agent, system
  senderId       String?             @db.ObjectId // AdminDB id if agent
  senderName     String?
  // Message content
  messageType    String              @default("text") // text, image, file, video, audio, location, card, richMenu
  content        String              // Text content or JSON for rich messages
  attachments    Json?               // Array of attachment objects
  metadata       Json?               // Additional metadata
  // Status
  isRead         Boolean             @default(false)
  readAt         DateTime?           @db.Date
  isSent         Boolean             @default(true)
  sentAt         DateTime            @default(now()) @db.Date
  // Auto-reply tracking
  isAutoReply    Boolean             @default(false)
  autoReplyId    String?             @db.ObjectId
  // Timestamps
  createdAt      DateTime            @default(now()) @db.Date
  updatedAt      DateTime            @updatedAt @db.Date

  @@index([conversationId])
  @@index([customerId])
  @@index([senderType])
  @@index([messageType])
  @@index([createdAt])
  @@index([isRead])
}

// Chat Tag - for categorizing customers
model ChatTagDB {
  id          String              @id @default(auto()) @map("_id") @db.ObjectId
  name        String              @unique
  color       String              @default("#3B82F6") // Hex color code
  description String?
  isActive    Boolean             @default(true)
  createdAt   DateTime            @default(now()) @db.Date
  createdBy   String
  updatedAt   DateTime            @updatedAt @db.Date
  updatedBy   String
  // Relations
  customers   ChatCustomerTagDB[]

  @@index([isActive])
}

// Join table for Customer-Tag relationship
model ChatCustomerTagDB {
  id         String         @id @default(auto()) @map("_id") @db.ObjectId
  customerId String         @db.ObjectId
  customer   ChatCustomerDB @relation(fields: [customerId], references: [id], onDelete: Cascade)
  tagId      String         @db.ObjectId
  tag        ChatTagDB      @relation(fields: [tagId], references: [id], onDelete: Cascade)
  createdAt  DateTime       @default(now()) @db.Date
  createdBy  String

  @@unique([customerId, tagId])
  @@index([customerId])
  @@index([tagId])
}

// Chat Segment - for dynamic customer grouping
model ChatSegmentDB {
  id          String                  @id @default(auto()) @map("_id") @db.ObjectId
  name        String                  @unique
  description String?
  // Segment rules (stored as JSON)
  rules       Json                    // Filter conditions
  isActive    Boolean                 @default(true)
  createdAt   DateTime                @default(now()) @db.Date
  createdBy   String
  updatedAt   DateTime                @updatedAt @db.Date
  updatedBy   String
  // Relations
  customers   ChatCustomerSegmentDB[]
  broadcasts  ChatBroadcastDB[]

  @@index([isActive])
}

// Join table for Customer-Segment relationship
model ChatCustomerSegmentDB {
  id         String         @id @default(auto()) @map("_id") @db.ObjectId
  customerId String         @db.ObjectId
  customer   ChatCustomerDB @relation(fields: [customerId], references: [id], onDelete: Cascade)
  segmentId  String         @db.ObjectId
  segment    ChatSegmentDB  @relation(fields: [segmentId], references: [id], onDelete: Cascade)
  createdAt  DateTime       @default(now()) @db.Date
  updatedAt  DateTime       @updatedAt @db.Date

  @@unique([customerId, segmentId])
  @@index([customerId])
  @@index([segmentId])
}

// Auto Reply - automated responses
model ChatAutoReplyDB {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  name        String
  description String?
  // Trigger conditions
  triggerType String   // keyword, welcome, away, default
  keywords    String[] // For keyword triggers
  // Response
  messageType String   @default("text") // text, image, card, richMenu
  message     String   // Text or JSON content
  attachments Json?    // Optional attachments
  // Scheduling
  isActive    Boolean  @default(true)
  startTime   String?  // HH:mm format
  endTime     String?  // HH:mm format
  daysOfWeek  String[] // ["monday", "tuesday", ...]
  // Priority (higher number = higher priority)
  priority    Int      @default(0)
  // Timestamps
  createdAt   DateTime @default(now()) @db.Date
  createdBy   String
  updatedAt   DateTime @updatedAt @db.Date
  updatedBy   String

  @@index([triggerType])
  @@index([isActive])
  @@index([priority])
}

// Rich Menu - interactive menu for chat widget
model ChatRichMenuDB {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  name        String   @unique
  description String?
  // Menu structure (JSON)
  menuData    Json     // Contains buttons, actions, layout
  imageUrl    String?  // Optional background image
  // Status
  isActive    Boolean  @default(false)
  isDefault   Boolean  @default(false) // Only one can be default
  // Timestamps
  createdAt   DateTime @default(now()) @db.Date
  createdBy   String
  updatedAt   DateTime @updatedAt @db.Date
  updatedBy   String

  @@index([isActive])
  @@index([isDefault])
}

// Broadcast - mass messaging
model ChatBroadcastDB {
  id           String                     @id @default(auto()) @map("_id") @db.ObjectId
  name         String
  description  String?
  // Target audience
  targetType   String                     // all, segment, tag, custom
  segmentId    String?                    @db.ObjectId
  segment      ChatSegmentDB?             @relation(fields: [segmentId], references: [id], onDelete: SetNull)
  customFilter Json?                      // Custom filter conditions
  // Message content
  messageType  String                     @default("text") // text, image, card
  message      String
  attachments  Json?
  // Scheduling
  status       String                     @default("draft") // draft, scheduled, sending, sent, failed
  scheduledAt  DateTime?                  @db.Date
  sentAt       DateTime?                  @db.Date
  // Statistics
  totalTarget  Int                        @default(0)
  totalSent    Int                        @default(0)
  totalFailed  Int                        @default(0)
  // Timestamps
  createdAt    DateTime                   @default(now()) @db.Date
  createdBy    String
  updatedAt    DateTime                   @updatedAt @db.Date
  updatedBy    String
  // Relations
  recipients   ChatBroadcastRecipientDB[]

  @@index([status])
  @@index([scheduledAt])
  @@index([createdAt])
}

// Broadcast Recipient - tracking individual broadcast sends
model ChatBroadcastRecipientDB {
  id           String          @id @default(auto()) @map("_id") @db.ObjectId
  broadcastId  String          @db.ObjectId
  broadcast    ChatBroadcastDB @relation(fields: [broadcastId], references: [id], onDelete: Cascade)
  customerId   String          @db.ObjectId
  // Status
  status       String          @default("pending") // pending, sent, failed, read
  errorMessage String?
  sentAt       DateTime?       @db.Date
  readAt       DateTime?       @db.Date
  // Timestamps
  createdAt    DateTime        @default(now()) @db.Date
  updatedAt    DateTime        @updatedAt @db.Date

  @@unique([broadcastId, customerId])
  @@index([broadcastId])
  @@index([customerId])
  @@index([status])
}

// Chat Widget Settings
model ChatWidgetSettingsDB {
  id                String   @id @default(auto()) @map("_id") @db.ObjectId
  // Appearance
  primaryColor      String   @default("#3B82F6")
  accentColor       String   @default("#10B981")
  headerTitle       String   @default("Chat with us")
  headerSubtitle    String?  @default("We're here to help")
  welcomeMessage    String?  @default("Hello! How can we help you today?")
  placeholderText   String   @default("Type a message...")
  // Widget behavior
  position          String   @default("bottom-right") // bottom-right, bottom-left
  autoOpen          Boolean  @default(false)
  showAgentAvatar   Boolean  @default(true)
  showTimestamp     Boolean  @default(true)
  enableFileUpload  Boolean  @default(true)
  enableEmoji       Boolean  @default(true)
  // Business hours
  businessHours     Json?    // {monday: {start: "09:00", end: "18:00"}, ...}
  offlineMessage    String?  @default("We're currently offline. Leave a message and we'll get back to you.")
  // Rich menu
  richMenuId        String?  @db.ObjectId
  // Timestamps
  createdAt         DateTime @default(now()) @db.Date
  createdBy         String
  updatedAt         DateTime @updatedAt @db.Date
  updatedBy         String
}
