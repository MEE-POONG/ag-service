generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

/**
 * ---------- TYPES (คงตามของเก่า ไม่แก้ฟิลด์ย่อย) ----------
 */

type AdjustbetsCockfight {
  /// Multiple data types found: Int: 26.2%, Int: 73.8% out of 65 sampled entries
  commissionRBG Json
  enabled       Boolean
  lastError     String?
  lastSavedAt   DateTime? @db.Date
  profile       Int
  statusServe   String?
  work          Boolean
}

type AdjustbetsLottoRcw {
  enabled     Boolean
  lastSavedAt DateTime? @db.Date
  share       Int
  statusServe String?
  work        Boolean
}

type AdjustbetsLottoRdc {
  enabled     Boolean
  lastSavedAt DateTime? @db.Date
  share       Int
  statusServe String?
  work        Boolean
}

type AdjustbetsMuayStep {
  enabled     Boolean
  /// Could not determine type: the field only had null or empty values in the sample set.
  lastError   Json?
  lastSavedAt DateTime? @db.Date
  profile     Int
  statusServe String?
  work        Boolean
}

type AdjustbetsSa {
  /// Multiple data types found: Int: 26.2%, Int: 73.8% out of 65 sampled entries
  commissionRAR Json
  enabled       Boolean
  lastError     String?
  lastSavedAt   DateTime? @db.Date
  profile       Int
  statusServe   String?
  work          Boolean
}

type AdjustbetsSexy {
  enabled     Boolean
  lastError   String?
  lastSavedAt DateTime? @db.Date
  profile     Int
  statusServe String?
  work        Boolean
}

type AdjustbetsSlotItp {
  enabled     Boolean
  lastError   String?
  lastSavedAt DateTime? @db.Date
  statusServe String?
  work        Boolean
}

type AdjustbetsSlotJoker {
  enabled     Boolean
  lastError   String?
  lastSavedAt DateTime? @db.Date
  statusServe String?
  work        Boolean
}

type AdjustbetsSlotPlaystar {
  enabled Boolean
  work    Boolean
}

type AdjustbetsSportsbook {
  commission  AdjustbetsSportsbookCommission
  lastError   String?
  lastSavedAt DateTime?                      @db.Date
  limits      AdjustbetsSportsbookLimits
  statusServe String?
  work        Boolean
}

type AdjustbetsSportsbookCommission {
  main  Int
  other Int
  par   Int
  x12   Int
}

type AdjustbetsSportsbookLimits {
  beforeRun       Int
  matchLimitOS    Int
  matchLimitOther Int
  matchLimitX12   Int
  maxOS           Int
  maxOther        Int
  maxPar          Int
  maxX12          Int
  par             Int
  transLimit      Int
}

type AdjustbetsVirtualSports {
  enabled     Boolean
  lastSavedAt DateTime? @db.Date
  profile     Int
  statusServe String?
  work        Boolean
}

type AgentWinLoseReportDbMetadata {
  columnKeys     String[]
  completedAt    String
  masterHeaders  String[]
  masterRecords  AgentWinLoseReportDbMetadataMasterRecords[]
  masterRowCount Int
  masterRows     Json
}

type AgentWinLoseReportDbMetadataMasterRecords {
  account                      String
  betAmount                    String
  betCount                     String
  commissionTotal              String
  companyCommission            String
  companyRollingAmount         String
  companyWinLose               String
  companyWinLoseWithCommission String
  contact                      String
  currency                     String
  memberCommission             String
  memberCount                  String
  memberWinLose                String
  memberWinLoseWithCommission  String
  rollingAmount                String
  seniorCommission             String
  seniorRollingAmount          String
  seniorWinLose                String
  seniorWinLoseWithCommission  String
}

type AgentWinLoseReportDbResults {
  headers        String[]
  masterAccount  String
  metadata       AgentWinLoseReportDbResultsMetadata
  records        AgentWinLoseReportDbResultsRecords[]
  reportUrl      String
  rowCount       Int
  rows           Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryHeaders Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryRecords Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryRows    Json?
}

type AgentWinLoseReportDbResultsMetadata {
  capturedAt String?
  columnKeys String[]
  error      String?
}

type AgentWinLoseReportDbResultsRecords {
  account                      String
  betAmount                    String
  betCount                     String
  commissionTotal              String
  companyCommission            String
  companyRollingAmount         String
  companyWinLose               String
  companyWinLoseWithCommission String
  contact                      String
  currency                     String
  memberCommission             String
  memberCount                  String
  memberWinLose                String
  memberWinLoseWithCommission  String
  rollingAmount                String
  seniorCommission             String
  seniorRollingAmount          String
  seniorWinLose                String
  seniorWinLoseWithCommission  String
}

type ChatWidgetKeyDbSettings {
  accentColor      String
  autoOpen         Boolean
  enableEmoji      Boolean
  enableFileUpload Boolean
  headerSubtitle   String
  headerTitle      String
  placeholderText  String
  position         String
  primaryColor     String
  showAgentAvatar  Boolean
  showTimestamp    Boolean
  welcomeMessage   String
}

type CreditTopUpAdviserDetails {
  createdAt   String
  createdBy   String
  gaSecretEnc String
  id          String
  isActive    Boolean
  /// Could not determine type: the field only had null or empty values in the sample set.
  meta        Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  note        Json?
  origin      String
  position    String
  reserve     String
  statusServe String
  updatedAt   String
  updatedBy   String
  userLogin   String
  username    String
  webname     String
}

type CustomerWinLoseReportDbMetadata {
  agentOverview         CustomerWinLoseReportDbMetadataAgentOverview[]
  agentSummaryCount     Int
  columnKeys            String[]
  completedAt           String
  customerSummaryCount  Int
  filters               CustomerWinLoseReportDbMetadataFilters
  masterHeaders         String[]
  masterRecords         CustomerWinLoseReportDbMetadataMasterRecords[]
  masterRowCount        Int
  masterRows            Json
  reportUrl             String
  totalAgentAccounts    Int
  totalCustomerRowCount Int
}

type CustomerWinLoseReportDbMetadataAgentOverview {
  agentCount    Int
  masterAccount String
  rowCount      Int
}

type CustomerWinLoseReportDbMetadataFilters {
  appliedButtons String[]
  endDate        String
  raw            CustomerWinLoseReportDbMetadataFiltersRaw
  startDate      String
}

type CustomerWinLoseReportDbMetadataFiltersRaw {
  categorySelectors String
  endSelectors      String
  selectedAll       String
  startSelectors    String
  submitSelector    String
}

type CustomerWinLoseReportDbMetadataMasterRecords {
  account                      String
  betAmount                    String
  betCount                     String
  commissionTotal              String
  companyCommission            String
  companyRollingAmount         String
  companyWinLose               String
  companyWinLoseWithCommission String
  contact                      String
  currency                     String
  memberCommission             String
  memberCount                  String
  memberWinLose                String
  memberWinLoseWithCommission  String
  rollingAmount                String
  seniorCommission             String
  seniorRollingAmount          String
  seniorWinLose                String
  seniorWinLoseWithCommission  String
}

type CustomerWinLoseReportDbResults {
  agentAccount   String
  headers        String[]
  masterAccount  String
  metadata       CustomerWinLoseReportDbResultsMetadata
  records        CustomerWinLoseReportDbResultsRecords[]
  reportUrl      String
  rowCount       Int
  rows           Json
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryHeaders Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryRecords Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryRows    Json?
}

type CustomerWinLoseReportDbResultsMetadata {
  capturedAt     String
  columnKeys     String[]
  seniorUsername String
}

type CustomerWinLoseReportDbResultsRecords {
  account                      String
  betAmount                    String
  betCount                     String
  commissionTotal              String
  companyCommission            String
  companyRollingAmount         String
  companyWinLose               String
  companyWinLoseWithCommission String
  contact                      String
  currency                     String
  memberCommission             String
  memberCount                  String
  memberWinLose                String
  memberWinLoseWithCommission  String
  rollingAmount                String
  seniorCommission             String
  seniorRollingAmount          String
  seniorWinLose                String
  seniorWinLoseWithCommission  String
}

type MasterWinLoseReportDbFilters {
  appliedButtons String[]
  endDate        String
  raw            MasterWinLoseReportDbFiltersRaw
  startDate      String
}

type MasterWinLoseReportDbFiltersRaw {
  categorySelectors String
  endSelectors      String
  selectedAll       String
  startSelectors    String
  submitSelector    String
}

type MasterWinLoseReportDbMetadata {
  columnKeys      String[]
  dataRowCount    Int?
  loginUsername   String
  selectedAccount String
  summaryRowCount Int?
  timestamp       String
  totalTables     Int
  webname         String
}

type MasterWinLoseReportDbRecords {
  account                      String
  betAmount                    String
  betCount                     String
  commissionTotal              String
  companyCommission            String?
  companyRollingAmount         String?
  companyWinLose               String?
  companyWinLoseWithCommission String?
  contact                      String
  currency                     String
  memberCommission             String
  memberCount                  String
  memberWinLose                String
  memberWinLoseWithCommission  String?
  rollingAmount                String
  seniorCommission             String?
  seniorRollingAmount          String?
  seniorWinLose                String?
  seniorWinLoseWithCommission  String?
}

/**
 * ---------- MODELS (อิงของเก่า + คง/รวมจากของใหม่ เฉพาะที่ไม่ซ้ำ) ----------
 */

model AGUserDB {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  adviser   String
  createdAt DateTime @db.Date
  createdBy String
  isActive  Boolean
  isDeleted Boolean
  password  String
  updatedAt DateTime @db.Date
  updatedBy String
  userAg    String   @unique
  userLogin String   @unique
  webBaseId String   @db.ObjectId
}

model ActivityLogDB {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  action    String
  createdAt DateTime @db.Date
  ipAddress String?
  newData   String?
  recordId  String?
  tableName String?
  userAgent String?
  userId    String
  userType  String
}

model AdminDB {
  id              String           @id @default(auto()) @map("_id") @db.ObjectId
  createdAt       DateTime         @db.Date
  createdBy       String
  deleteAt        DateTime?        @db.Date
  /// Could not determine type: the field only had null or empty values in the sample set.
  deleteBy        Json?
  email           String           @unique
  isActive        Boolean
  name            String
  password        String
  /// Could not determine type: the field only had null or empty values in the sample set.
  profile         Json?
  tel             String
  updatedAt       DateTime         @db.Date
  updatedBy       String
  username        String           @unique
  AdminPositionDB AdminPositionDB? @relation(fields: [AdminPositionDBId], references: [id])
  AdminPositionDBId String?          @db.ObjectId
  WebBaseDB       WebBaseDB?       @relation(fields: [webBaseDBId], references: [id])
  webBaseDBId     String?          @db.ObjectId
  // Chat system relations
  assignedConversations ChatConversationDB[]
  // Widget keys relations
  widgetKeys        ChatWidgetKeyDB[]
  // Message read tracking
  messageReads      ChatMessageReadDB[]
  // Notification system relations
  notifications     NotificationDB[]
  pushSubscriptions PushSubscriptionDB[]
  notificationPreferences NotificationPreferencesDB?
}

model AdminDefaultPermissionDB {
  id                String          @id @default(auto()) @map("_id") @db.ObjectId
  AdminPositionDBId String          @db.ObjectId
  canAdvance        Boolean
  canCreate         Boolean
  canDelete         Boolean
  canUpdate         Boolean
  canViews          Boolean
  createdAt         DateTime        @db.Date
  createdBy         String
  isDeleted         Boolean
  menuPageWebId     String?         @db.ObjectId
  updatedAt         DateTime        @db.Date
  updatedBy         String
  AdminPositionDB   AdminPositionDB @relation(fields: [AdminPositionDBId], references: [id])
  MenuPageWebDB     MenuPageWebDB?  @relation(fields: [menuPageWebDBId], references: [id])
  menuPageWebDBId   String?         @db.ObjectId

  @@unique([AdminPositionDBId, menuPageWebId])
}

model AdminDepartmentDB {
  id              String            @id @default(auto()) @map("_id") @db.ObjectId
  createdAt       DateTime          @db.Date
  createdBy       String
  description     String
  isActive        Boolean
  isDeleted       Boolean
  name            String            @unique
  updatedAt       DateTime          @db.Date
  updatedBy       String
  AdminPositionDB AdminPositionDB[]
}

model AdminPositionDB {
  id                       String                     @id @default(auto()) @map("_id") @db.ObjectId
  adminDepartmentId        String                     @db.ObjectId
  createdAt                DateTime                   @db.Date
  createdBy                String
  isActive                 Boolean
  isDeleted                Boolean
  name                     String
  priority                 Int
  updatedAt                DateTime                   @db.Date
  updatedBy                String
  adminDepartment          AdminDepartmentDB          @relation(fields: [adminDepartmentId], references: [id], onDelete: Cascade)
  AdminDB                  AdminDB[]
  AdminDefaultPermissionDB AdminDefaultPermissionDB[]

  @@unique([adminDepartmentId, name], map: "uniq_position_name_per_dept")
  @@index([adminDepartmentId])
}

model AgQueueJobDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String?
  createdAt    DateTime @db.Date
  credit       Int?
  creditBy     String?
  customer     String?
  endDate      String?
  errorMessage String?
  errorStack   String?
  eventDocId   String
  jobId        String   @unique(map: "jobId_1")
  partnerAG    String?
  partnerLogin String?
  priority     Int
  queueSize    Int
  reportUrl    String?
  rowCount     Int?
  startDate    String?
  status       String
  statusAG     String?
  type         String
  updatedAt    DateTime @db.Date
  userLogin    String?
  username     String?
}

model AgUserAccountDB {
  id              String            @id @default(auto()) @map("_id") @db.ObjectId
  v               Int?              @map("__v")
  createdAt       DateTime          @db.Date
  createdBy       String
  gaSecretEnc     String
  isActive        Boolean
  /// Could not determine type: the field only had null or empty values in the sample set.
  meta            Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  note            Json?
  origin          String
  partnerAG       String?
  partnerLogin    String?
  position        String
  reserve         String
  statusServe     String
  updatedAt       DateTime          @db.Date
  updatedBy       String
  userLogin       String
  username        String            @unique
  webname         String
  CustomerAgentDB CustomerAgentDB[]
  PartnerDB       PartnerDB[]
}

model AgentChangePasswordDB {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  v           Int      @map("__v")
  adviser     String
  agentLogin  String
  createdAt   DateTime @db.Date
  newPassword String
  oldPassword String
  processedAt DateTime @db.Date
  status      String
  updatedAt   DateTime @db.Date
  usernameAG  String
}

model AgentWinLoseReportDB {
  id           String                        @id @default(auto()) @map("_id") @db.ObjectId
  adviser      String
  endDate      String
  errorMessage String?
  metadata     AgentWinLoseReportDbMetadata?
  position     String
  processedAt  DateTime?                     @db.Date
  results      AgentWinLoseReportDbResults[]
  startDate    String
  status       String
  updatedAt    DateTime?                     @db.Date
  usernameAG   String
  webname      String
}

model ChatAutoReplyDB {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  isActive    Json?
  /// Field referred in an index, but found no data to define the type.
  priority    Json?
  /// Field referred in an index, but found no data to define the type.
  triggerType Json?

  @@index([triggerType])
  @@index([isActive])
  @@index([priority])
}

model ChatBroadcastDB {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  createdAt   Json?
  /// Field referred in an index, but found no data to define the type.
  scheduledAt Json?
  /// Field referred in an index, but found no data to define the type.
  status      Json?

  @@index([status])
  @@index([scheduledAt])
  @@index([createdAt])
}

model ChatBroadcastRecipientDB {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  broadcastId Json?
  /// Field referred in an index, but found no data to define the type.
  customerId  Json?
  /// Field referred in an index, but found no data to define the type.
  status      Json?

  @@unique([broadcastId, customerId])
  @@index([broadcastId])
  @@index([customerId])
  @@index([status])
}

model ChatConversationDB {
  id              String    @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  assignedAdminId Json?
  createdAt       DateTime  @db.Date
  customerId      String    @db.ObjectId
  isUnread        Boolean
  lastMessage     String?
  lastMessageAt   DateTime? @db.Date
  priority        String
  status          String
  subject         String
  unreadCount     Int
  updatedAt       DateTime  @db.Date
  widgetKeyId     String?   @db.ObjectId

  @@index([customerId])
  @@index([assignedAdminId])
  @@index([widgetKeyId])
  @@index([status])
  @@index([priority])
  @@index([createdAt])
  @@index([lastMessageAt])
}

model ChatCustomerDB {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt   DateTime @db.Date
  customerId  String   @unique
  email       String?
  isActive    Boolean
  isBlocked   Boolean
  language    String
  /// Field referred in an index, but found no data to define the type.
  lastLoginAt Json?
  /// Nested objects had no data in the sample dataset to introspect a nested type.
  metadata    Json
  name        String
  /// Field referred in an index, but found no data to define the type.
  phone       Json?
  /// Field referred in an index, but found no data to define the type.
  source      Json?
  timezone    String
  updatedAt   DateTime @db.Date

  @@index([email])
  @@index([phone])
  @@index([isActive])
  @@index([createdAt])
  @@index([source])
  @@index([lastLoginAt])
}

model ChatCustomerSegmentDB {
  id         String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  customerId Json?
  /// Field referred in an index, but found no data to define the type.
  segmentId  Json?

  @@unique([customerId, segmentId])
  @@index([customerId])
  @@index([segmentId])
}

model ChatCustomerTagDB {
  id         String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  customerId Json?
  /// Field referred in an index, but found no data to define the type.
  tagId      Json?

  @@unique([customerId, tagId])
  @@index([customerId])
  @@index([tagId])
}

model ChatMessageDB {
  id             String   @id @default(auto()) @map("_id") @db.ObjectId
  /// Could not determine type: the field only had null or empty values in the sample set.
  attachments    Json?
  content        String
  conversationId String   @db.ObjectId
  createdAt      DateTime @db.Date
  customerId     String   @db.ObjectId
  isAutoReply    Boolean
  isRead         Boolean
  isSent         Boolean
  messageType    String
  /// Could not determine type: the field only had null or empty values in the sample set.
  metadata       Json?
  senderId       String   @db.ObjectId
  senderName     String
  senderType     String
  sentAt         DateTime @db.Date
  updatedAt      DateTime @db.Date
  widgetKeyId    String?  @db.ObjectId

  @@index([conversationId])
  @@index([customerId])
  @@index([widgetKeyId])
  @@index([senderType])
  @@index([messageType])
  @@index([createdAt])
  @@index([isRead])
}

model ChatMessageReadDB {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  adminId   Json?
  /// Field referred in an index, but found no data to define the type.
  messageId Json?
  /// Field referred in an index, but found no data to define the type.
  readAt    Json?

  @@unique([messageId, adminId])
  @@index([messageId])
  @@index([adminId])
  @@index([readAt])
}

model ChatRichMenuDB {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  isActive  Json?
  /// Field referred in an index, but found no data to define the type.
  isDefault Json?
  /// Field referred in an index, but found no data to define the type.
  name      Json?  @unique

  @@index([isActive])
  @@index([isDefault])
}

model ChatSegmentDB {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  isActive Json?
  /// Field referred in an index, but found no data to define the type.
  name     Json?  @unique

  @@index([isActive])
}

model ChatTagDB {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  isActive Json?
  /// Field referred in an index, but found no data to define the type.
  name     Json?  @unique

  @@index([isActive])
}

model ChatWidgetKeyDB {
  id          String                  @id @default(auto()) @map("_id") @db.ObjectId
  createdAt   DateTime                @db.Date
  createdBy   String                  @db.ObjectId
  description String
  domain      String
  isActive    Boolean
  key         String                  @unique
  lastUsedAt  DateTime                @db.Date
  name        String
  settings    ChatWidgetKeyDbSettings
  updatedAt   DateTime                @db.Date

  @@index([domain])
  @@index([isActive])
  @@index([createdAt])
}

model ChatWidgetSettingsDB {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model CreateAgentDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String
  createdAt    DateTime @db.Date
  errorMessage String?
  errorStack   String?
  position     String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

model CreateMasterAgentDB {
  id           String    @id @default(auto()) @map("_id") @db.ObjectId
  v            Int       @map("__v")
  adviser      String
  createdAt    DateTime  @db.Date
  errorMessage String?
  errorStack   String?
  position     String
  processedAt  DateTime? @db.Date
  status       String
  updatedAt    DateTime  @db.Date
  usernameAG   String
}

model CreatePartnerDB {
  id                     String   @id @default(auto()) @map("_id") @db.ObjectId
  v                      Int      @map("__v")
  adviser                String
  createdAt              DateTime @db.Date
  createdPartnerNickname String?
  createdPartnerPassword String?
  createdPartnerUsername String?
  errorMessage           String
  parentUsername         String
  position               String
  processedAt            DateTime @db.Date
  status                 String
  updatedAt              DateTime @db.Date
  usernameAG             String
}

model CreateResetMasterJobsDB {
  id            String   @id @default(auto()) @map("_id") @db.ObjectId
  /// Multiple data types found: Int: 31.6%, Int: 68.4% out of 19 sampled entries
  v             Json     @map("__v")
  adviser       String
  createdAt     DateTime @db.Date
  errorMessage  String?
  errorStack    String?
  insertedCount Int
  /// Multiple data types found: Int: 31.6%, Int: 68.4% out of 19 sampled entries
  limit         Json
  newPassword   String
  processedAt   DateTime @db.Date
  status        String
  updatedAt     DateTime @db.Date
}

model CustomerAgentDB {
  id              String          @id @default(auto()) @map("_id") @db.ObjectId
  agentId         String          @db.ObjectId
  agUserAccountDB AgUserAccountDB @relation(fields: [agentId], references: [id], onDelete: Cascade)
  createdAt       DateTime        @db.Date
  createdBy       String
  customerId      String          @unique
  isActive        Boolean
  note            String
  updatedAt       DateTime        @db.Date
  updatedBy       String
}

model CustomerWinLoseReportDB {
  id          String                           @id @default(auto()) @map("_id") @db.ObjectId
  adviser     String
  endDate     String
  metadata    CustomerWinLoseReportDbMetadata
  position    String
  processedAt DateTime                         @db.Date
  results     CustomerWinLoseReportDbResults[]
  startDate   String
  status      String
  updatedAt   DateTime                         @db.Date
  usernameAG  String
  webname     String
}

model ImageList {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime @db.Date
  createdBy String
  deleteBy  String
  imageUrl  String
  isDeleted Boolean
  modelName String
  nameFile  String
  updatedAt DateTime @db.Date
  updatedBy String
}

model MasterChangePasswordDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String
  createdAt    DateTime @db.Date
  errorMessage String?
  errorStack   String?
  masterLogin  String
  newPassword  String
  oldPassword  String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

model MasterWinLoseReportDB {
  id             String                         @id @default(auto()) @map("_id") @db.ObjectId
  v              Int?                           @map("__v")
  adviser        String
  createdAt      DateTime?                      @db.Date
  endDate        String
  filters        MasterWinLoseReportDbFilters?
  headers        String[]
  metadata       MasterWinLoseReportDbMetadata?
  position       String
  processedAt    DateTime?                      @db.Date
  records        MasterWinLoseReportDbRecords[]
  reportUrl      String?
  rowCount       Int?
  rows           Json?
  startDate      String
  status         String?
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryHeaders Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryRecords Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  summaryRows    Json?
  updatedAt      DateTime?                      @db.Date
  usernameAG     String
  webname        String
}

model MenuPageWebDB {
  id                       String                     @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  link                     Json?                      @unique
  /// Field referred in an index, but found no data to define the type.
  name                     Json?                      @unique
  AdminDefaultPermissionDB AdminDefaultPermissionDB[]
}

model MenuWebDB {
  id          String      @id @default(auto()) @map("_id") @db.ObjectId
  canAdvance  Boolean
  canCreate   Boolean
  canDelete   Boolean
  canUpdate   Boolean
  canViews    Boolean
  createdAt   DateTime    @db.Date
  createdBy   String
  description String
  head        Boolean
  icon        String?
  isDeleted   Boolean?
  isVisible   Boolean
  link        String
  manager     String[]
  name        String      @unique
  parentId    String?     @db.ObjectId
  showOrder   Int
  updatedAt   DateTime    @db.Date
  updatedBy   String
  // Self-referential relations for menu hierarchy
  parent      MenuWebDB?  @relation("MenuParentChildren", fields: [parentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  children    MenuWebDB[] @relation("MenuParentChildren")
}

model NotificationDB {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  createdAt Json?
  /// Field referred in an index, but found no data to define the type.
  isRead    Json?
  /// Field referred in an index, but found no data to define the type.
  type      Json?
  /// Field referred in an index, but found no data to define the type.
  userId    Json?

  @@index([userId])
  @@index([isRead])
  @@index([createdAt])
  @@index([type])
}

model NotificationPreferencesDB {
  id     String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  userId Json?  @unique
}

model PartnerDB {
  id                String           @id @default(auto()) @map("_id") @db.ObjectId
  agentId           String           @db.ObjectId
  bankName          String
  bankNumber        String
  createdAt         DateTime         @db.Date
  createdBy         String
  line              String
  method            String
  name              String
  startDate         DateTime         @db.Date
  status            String
  tel               String
  updatedAt         DateTime         @db.Date
  updatedBy         String
  AgUserAccountDB   AgUserAccountDB? @relation(fields: [agUserAccountDBId], references: [id])
  agUserAccountDBId String?          @db.ObjectId
}

model PushSubscriptionDB {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  /// Field referred in an index, but found no data to define the type.
  endpoint Json?  @unique
  /// Field referred in an index, but found no data to define the type.
  isActive Json?
  /// Field referred in an index, but found no data to define the type.
  userId   Json?

  @@index([userId])
  @@index([isActive])
}

model ResetAccountPartnerDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String
  createdAt    DateTime @db.Date
  errorMessage String
  errorStack   String?
  partnerAG    String
  partnerLogin String
  position     String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

model ResetAgUserPasswordDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  v            Int      @map("__v")
  adviser      String
  createdAt    DateTime @db.Date
  errorMessage String?
  newPassword  String
  position     String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

model SetZero {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model SettingDB {
  id                String   @id @default(auto()) @map("_id") @db.ObjectId
  name              String
  logo              String
  logoText          String?
  logoTextColor     String
  logoTextSize      Int
  logoTextFont      String
  logoTextFontSize  Int
  logoTextFontColor String
  facebook          String?
  instagram         String?
  line              String?
  youtube           String?
  website           String?
  address           String?
  tel               String
  createdAt         DateTime @db.Date
  createdBy         String
  updatedAt         DateTime @db.Date
  updatedBy         String
}

model UnblockAgentDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  /// Multiple data types found: Int: 71.4%, Int: 28.6% out of 7 sampled entries
  v            Json     @map("__v")
  adviser      String
  createdAt    DateTime @db.Date
  errorMessage String
  newPassword  String
  position     String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

model UnblockCustomerDB {
  id               String   @id @default(auto()) @map("_id") @db.ObjectId
  adviser          String
  createdAt        String
  createdBy        String
  customerUsername String?
  errorMessage     String
  /// Could not determine type: the field only had null or empty values in the sample set.
  errorStack       Json?
  ipAddress        String
  newPassword      String
  processedAt      DateTime @db.Date
  requestedBy      String
  status           String
  type             String
  updatedAt        DateTime @db.Date
  userAgent        String
  usernameAG       String
}

model UnblockMasterDB {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  /// Multiple data types found: Int: 21.4%, Int: 78.6% out of 28 sampled entries
  v            Json     @map("__v")
  adviser      String
  createdAt    DateTime @db.Date
  errorMessage String
  newPassword  String
  position     String
  processedAt  DateTime @db.Date
  status       String
  updatedAt    DateTime @db.Date
  usernameAG   String
}

model UserDB {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime @db.Date
  createdBy String
  email     String   @unique
  firstname String
  isActive  Boolean
  isDeleted Boolean
  lastname  String
  password  String
  tel       String
  updatedAt DateTime @db.Date
  updatedBy String
  username  String   @unique
}

model WebBaseDB {
  id        String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime  @db.Date
  createdBy String
  isActive  Boolean
  isDeleted Boolean
  name      String    @unique
  otpA      String
  otpM      String
  otpS      String
  passA     String
  passM     String
  passS     String
  updatedAt DateTime  @db.Date
  updatedBy String
  AdminDB   AdminDB[]

  @@index([isActive], map: "isActive_1")
}

model WorkHistoryDB {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  action    String
  createdAt DateTime @db.Date
  errorMsg  String?
  ipAddress String
  isSuccess Boolean
  newData   String?
  oldData   String?
  recordId  String?  @db.ObjectId
  tableName String
  userAgent String
  userId    String
  userType  String

  @@index([tableName])
  @@index([recordId])
  @@index([userId])
  @@index([action])
  @@index([createdAt])
}

model adjustbets {
  id            String                  @id @default(auto()) @map("_id") @db.ObjectId
  agBaseUrl     String
  /// Multiple data types found: Int: 26.2%, Int: 73.8% out of 65 sampled entries
  attemptCount  Json
  cockfight     AdjustbetsCockfight
  createdAt     DateTime                @db.Date
  createdBy     String
  customer      String
  lastError     String
  lottoRCW      AdjustbetsLottoRcw
  lottoRDC      AdjustbetsLottoRdc
  muayStep      AdjustbetsMuayStep
  overallStatus String
  pinUsed       String
  sa            AdjustbetsSa
  sexy          AdjustbetsSexy
  slotItp       AdjustbetsSlotItp
  slotJoker     AdjustbetsSlotJoker
  slotPlaystar  AdjustbetsSlotPlaystar
  sportsbook    AdjustbetsSportsbook
  updatedAt     DateTime                @db.Date
  updatedBy     String
  usernameAG    String
  virtualSports AdjustbetsVirtualSports
}

model admin {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  bankName   String
  bankNumber String
  createdAt  DateTime @db.Date
  /// Could not determine type: the field only had null or empty values in the sample set.
  deleted_at Json?
  firstname  String
  lastname   String
  line       String
  nickname   String
  password   String
  position   String
  team       String
  tel        String
  updatedAt  DateTime @db.Date
  username   String
}

model creditTopUp {
  id             String                     @id @default(auto()) @map("_id") @db.ObjectId
  adviser        String
  adviserDetails CreditTopUpAdviserDetails?
  agentId        String
  agentLogin     String
  createdAt      String
  createdBy      String
  credit         Int
  creditBy       String
  status         String
  statusAG       String?
  statusFlag     String
  statusServe    String
  type           String
  /// Multiple data types found: String: 3.5%, DateTime (Date): 96.5% out of 141 sampled entries
  updatedAt      Json
  updatedBy      String
  usernameAG     String
  webname        String
}
